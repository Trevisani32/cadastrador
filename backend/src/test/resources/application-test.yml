spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: ""              # chave aleatória em memória durante os testes
    expiration: 86400000
  cors:
    allowed-origins: http://localhost:4200
  admin:
    username: admin
    password: admin123
    email: admin@exemplo.com
  seed-demo-data: false     # sem clientes de demonstração nos testes
  recuperacao:
    expor-codigo: false
