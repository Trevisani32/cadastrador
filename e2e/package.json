{
  "name": "cadastrador-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes end-to-end (Playwright) do Cadastrador — exercita Angular + Spring Boot reais.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
